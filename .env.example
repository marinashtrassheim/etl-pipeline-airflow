# Copy to .env and fill in before running docker compose (docker compose reads .env automatically).
# Generate a Fernet key with:
#   python3 -c "import base64, os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
AIRFLOW_FERNET_KEY=
